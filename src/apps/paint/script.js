// JavaScript logic for Paint (Iframe base)
// Most functionality is handled within the jspaint.app iframe
// This script can handle communication if needed in the future

window.onload = () => {
    console.log("Paint loaded with JS Paint integration.");
};
